root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/main ./cmd/main"
  entrypoint = ["./tmp/main"]
  delay = 1000
  include_ext = ["go"]
  exclude_dir = ["tmp", "bin"]
  stop_on_error = true

[misc]
  clean_on_exit = true
